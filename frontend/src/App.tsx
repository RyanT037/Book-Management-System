import { AppRoutes } from './routes/AppRoutes';

// The root App component serves as the entry point for the application's UI.
// It delegates all routing logic to the AppRoutes component.
export default function App() {
  return <AppRoutes />;
}
