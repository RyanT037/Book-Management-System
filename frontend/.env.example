# API Configuration
# The Vite development server is configured to proxy /auth requests to http://localhost:3000
# For API calls that don't go through the proxy, you can set VITE_API_URL
VITE_API_URL=http://localhost:3000
